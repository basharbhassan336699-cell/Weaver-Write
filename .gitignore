# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# ── Weaver Write secrets & local state (never commit real keys) ──
config/.env
config/providers.json
~/.weaver-write/
*.db
weaver_memory.db
# generated output documents (written to disk by the pipeline)
outputs/
# saved chats/messages (persist on disk, never committed or wiped by updates)
config/chats/

# ── OS / editor noise ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
