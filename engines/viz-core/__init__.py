"""
viz-core — visualization & workflow engine.
Hosts vendored langgraph (pipeline workflow) and backs the `chart` tool
(matplotlib via requirements). Interactive JS chart libs live in
frontend-core/vendored (chart.js, d3).
"""
