# Heavy libraries not vendored (install via pip)
pandas>=2.0
pypdf>=4.0
reportlab>=4.0
python-docx>=1.1
python-pptx>=0.6

# Note: pandoc is a system binary — install via package manager, not pip
# Termux: pkg install pandoc

# Arabic PDF rendering (correct letter shaping + RTL order)
arabic-reshaper>=3.0
python-bidi>=0.4
