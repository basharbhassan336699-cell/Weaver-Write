"""
capabilities/libraries/ — Layer 3: code libraries
=================================================
These are not tools or skills, but documentation of the code libraries
called inside the tool code (Layer 1).

registry.json documents: library name, purpose, and which tools use it.

For full installation see requirements.txt at the project root.
"""
